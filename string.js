const isValid = 'title';
const isnotValid = 'titlE';

if (isValid.toLocaleLowerCase() === isnotValid.toLocaleLowerCase()){
    console.log('incase is valid');
    
}
else{
    console.log('incase is not valid num')
}